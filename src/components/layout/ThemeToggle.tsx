"use client";

import { useTheme } from "next-themes";
import { Moon, Sun } from "lucide-react";
import { useEffect, useState } from "react";

export function ThemeToggle() {
  // Theme toggle disabled because the site enforces a dark-only theme.
  return null;
}
