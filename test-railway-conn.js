const mariadb = require('mariadb'); const pool = mariadb.createPool({ host:'thomas.proxy.rlwy.net', port:57837, user:'root', password:'hFigVRvygRKgMrNoIfaUQySNhVlnyWHj', database:'railway', ssl:{rejectUnauthorized:false}, connectTimeout:20000, acquireTimeout:20000 }); pool.getConnection().then(conn => { console.log('connected'); return conn.end(); }).catch(err => { console.error('connect error', err); process.exit(1); });
